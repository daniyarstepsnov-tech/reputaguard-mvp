# main.py - Рабочий пример из официальной документации Botasaurus 4.0.88

from botasaurus.browser import browser, Driver

@browser
def scrape_heading_task(driver: Driver, data):
    """
    Собирает заголовок с веб-сайта (официальный пример из документации)
    """
    print("🔄 Открываем веб-сайт...")
    
    # Посетить веб-сайт Omkar Cloud
    driver.get("https://www.omkar.cloud/")
    
    # Получить текст элемента заголовка
    heading = driver.get_text("h1")
    
    print(f"✅ Заголовок найден: {heading}")
    
    # Сохранить данные
    return {"heading": heading}

if __name__ == "__main__":
    # Запуск задачи (важно: вызываем функцию напрямую)
    result = scrape_heading_task({})
    print(f"📊 Результат сбора данных: {result}")